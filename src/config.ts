// Single place holding the repo slug — used only server-side (build time) to fetch
// releases from the GitHub API in src/lib/releases.ts. The repo itself is private;
// no page should ever link to it or otherwise expose it to visitors.
export const GITHUB_REPO = 'Saksham1800t/SDE-Code-App';
export const PRODUCT_NAME = 'SDE Code';
export const TAGLINE = 'The AI-first IDE.';
