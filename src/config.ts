// Single place to point the whole site at the real GitHub repo once one
// exists (see this project's plan doc, Phase 3) — nothing else should
// hardcode "owner/repo" or a github.com URL.
export const GITHUB_REPO = 'Saksham1800t/SDE-Code-App';
export const GITHUB_URL = `https://github.com/${GITHUB_REPO}`;
export const PRODUCT_NAME = 'SDE Code';
export const TAGLINE = 'The AI-first IDE, built from scratch.';
